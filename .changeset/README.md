# Changesets

Cada cambio que afecte a un paquete publicado debe incluir un changeset:

```bash
pnpm changeset
```

Elige los paquetes afectados, el tipo de versión (patch / minor / major) y describe el cambio
pensando en quien usa la librería. Más info: https://github.com/changesets/changesets
